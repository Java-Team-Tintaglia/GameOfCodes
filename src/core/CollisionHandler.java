package core;


import models.GameObject;
import models.programmingLanguages.ProgrammingLanguage;
import models.students.Student;
import models.wizards.Wizard;
import states.GameState;

import java.awt.*;

public class CollisionHandler {


    public void collisionHandler(){

        for (GameObject entity : GameState.entities) {
            // TODO: to foreach all the entities-they must have boundbox 
         //   if(GameState.student.intersects())
        }

    }


}
